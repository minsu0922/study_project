package project.study.study_project.llm.client;

/**
 * 문제 생성의 <b>근거가 되는 개념 문서</b> — 2단계(docs/15).
 *
 * <p>4일 주기의 2·3·4일차에는 1일차에 만든 문서를 읽어 그 내용으로만 문제를 낸다.
 * 이 record는 그 문서를 프롬프트에 실어 나르는 그릇이다.
 *
 * <p><b>왜 근거를 주는 것이 중요한가.</b> 지금까지 모델은 자기 기억에서 문제를 뽑았다.
 * 그러면 그럴듯하지만 틀린 문제가 나올 수 있고(환각), 검수자가 사실 확인을 직접 해야 했다.
 * 근거 문서를 주면 <b>모델이 참조할 곳이 생기고 검수자도 대조할 곳이 생긴다</b> —
 * "이 문제가 맞나?"를 인터넷이 아니라 바로 위 문서에서 확인할 수 있다.
 *
 * <p>문서 전문을 그대로 싣는다(요약하지 않는다). 3,000자면 대략 2천 토큰이고 입력 토큰은
 * $5/1M이라 편당 1센트 남짓이다 — 요약하느라 근거를 잃는 것이 훨씬 비싸다.
 *
 * @param slug      문서 식별자. 생성 결과 파일과 초안에 기록해 "이 문제의 근거"를 남긴다
 * @param title     문서 제목 — 프롬프트에서 주제를 한 줄로 알려 주는 용도
 * @param contentMd 마크다운 본문 전문
 */
public record SourceDocument(String slug, String title, String contentMd) {
}
