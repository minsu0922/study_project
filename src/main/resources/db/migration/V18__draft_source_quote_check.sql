-- =====================================================================
-- V18__draft_source_quote_check.sql — 근거 인용 대조 결과를 초안에 남긴다
-- =====================================================================
-- 배경: 검수 화면의 품질 경고는 2026-08-25에 이미 붙었다. 조회할 때마다 저장된 값으로
-- 다시 재는 방식이라(LlmProblemService.toResponse) 규칙을 고치면 대기 중인 초안도 새 잣대를
-- 받는다 — 좋은 설계이고 그대로 둔다.
--
-- 딱 하나, 근거 인용 대조(SourceQuoteRule)만 그 방식으로 할 수 없어 빠져 있었다.
-- 재계산에 필요한 두 입력이 <검수 시점에는 존재하지 않기> 때문이다:
--   · sourceQuote      — 모델이 돌려준 값. 학습자에게 보일 일이 없어 초안에 저장하지 않는다
--   · 근거 문서 본문     — 업로드 경로는 파일이 요청과 함께 사라진다(서버에 남기지 않는다)
-- 그래서 배치 CLI만 이 검사를 했고, 관리 화면으로 뽑은 문제는 "이 문제가 정말 그 문서에서
-- 나왔는가"를 아무도 묻지 않았다. 문서를 근거로 뽑는 경로에서 가장 묻고 싶은 질문인데도.
--
-- 결론: 재계산할 수 없는 것만 <생성 시점에 찍어 둔다>. "파생 가능한 값은 저장하지 않는다"는
-- 이 저장소의 원칙에 대한 예외이고, 예외인 이유는 파생에 쓸 입력이 사라지기 때문이다.
-- 나머지 경고는 계속 조회 시점에 계산한다 — 두 벌로 저장하면 같은 경고가 두 번 뜬다.
--
-- 길이 500: 이 값은 문장 하나다("근거 인용을 문서에서 찾지 못함 (\"…\")"). 목록이 아니다.
-- =====================================================================

ALTER TABLE generated_problem_draft
    -- 문제가 없으면 NULL. "대조했고 깨끗함"과 "대조하지 않음(근거 문서 없이 만든 문제·옛 초안)"을
    -- 같은 NULL로 둔다 — 화면이 둘에게 할 말이 같고, 가르려면 컬럼이 하나 더 필요하다.
    ADD COLUMN source_quote_check VARCHAR(500) NULL AFTER model;
